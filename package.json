{
  "name": "gworkspace-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "REACT_APP_GOOGLE_SIGNIN_CLIENT_ID=\"$GOOGLE_OAUTH_CLIENT_ID\" REACT_APP_GOOGLE_MAPS_API_KEY=\"${GOOGLE_MAPS_API_KEY:-$GOOGLE_VOICE_API_KEY}\" npm run build --prefix frontend",
    "build": "npm run build:frontend",
    "start": "node backend/backend-server.js",
    "dev": "test -f frontend/build/index.html || npm run build:frontend; node backend/backend-server.js"
  }
}
