{
  "name": "gworkspace-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "REACT_APP_GOOGLE_SIGNIN_CLIENT_ID=\"$GOOGLE_OAUTH_CLIENT_ID\" npm run build --prefix frontend",
    "build": "npm run build:frontend",
    "dev": "npm run build:frontend && PORT=3000 HOST=0.0.0.0 node backend/backend-server.js"
  }
}
